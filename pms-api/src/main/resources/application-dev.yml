# 开发环境配置
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hotel_pms
    username: postgres
    password: postgres
  
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.hotel.pms: DEBUG
    org.springframework.transaction: DEBUG